scratch-desktop for Debian

Build process
~~~~~~~~~~~~~

To build this package you'll want to do something like this:

From a fresh Debian install:

```sh
sudo apt install devscripts build-essential git-buildpackage equivs --no-install-recommends
sudo mk-build-deps -i debian/control
gbp buildpackage --git-debian-branch=main
```

 -- Patrick J Cherry <patrick.cherry@raspberrypi.org>  Tue, 24 May 2022 21:27:38 +0100
